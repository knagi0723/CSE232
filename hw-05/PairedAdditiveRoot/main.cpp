#include <iostream>
#include <cmath>

using std::cin;
using std::cout;
using std::endl;

int main() {
    int num;
    while (cin>>num) {}
        while (num>100) {
            num = (num%100) + ((num/10000)%100);
            num1 = num + ((test/1000000)%100);
            num2 = num1 + (test/100)%100;
            num3 = num2 + (test/1000000000)%100;
        }
    }
return 0
}